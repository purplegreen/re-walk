// eslint-disable-next-line prettier/prettier
const {
  slots
} = require('../services/SlotService.js')

const state = {
  slots,
  error: false,
  errorMessage: '',
  success: false,
}

const mutations = {}

const actions = {}

export default {
  state,
  mutations,
  actions,
}
