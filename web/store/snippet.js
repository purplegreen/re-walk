// eslint-disable-next-line prettier/prettier
const {
  snippet
} = require('../sanity.js')

const state = {
  snippet,
  error: false,
  errorMessage: '',
  success: false,
}

const mutations = {}

const actions = {}

export default {
  state,
  mutations,
  actions,
}
